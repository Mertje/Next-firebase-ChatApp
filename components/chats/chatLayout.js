
const chatLayout = (groupUID) => {
    console.log(groupUID)
    // const messageRef = firestore.collection('chats')
    // const snapshot =  messageRef.where('groupUID', '==', true).get();
    
    return ( <div></div> );
}
 
export default chatLayout;